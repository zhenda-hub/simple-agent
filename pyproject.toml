[project]
name = "simple-agent"
version = "0.1.0"
description = "A simple AI agent with tool use and code execution"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.31.0",
    "python-dotenv>=1.2.2",
    "rich>=13.0.0",
]
